spring.application.name=pizzariae

server.port=8000
spring.datasource.url=jdbc:sqlserver//localhost:1433;databaseName=bd_virtual_VICTOR_2e;integratedSecurity=false;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=@ITB123456
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Informações

# IntegratedSecutiyy: (true) indica que a conexão será feita com as credenciais do windows (false) Credencial do SQL Server
# encrypt: (true) Requer criptografia do cliente (false) Não requer criptografia do Cliente
# trustServerCertificate: (true) Instrue o aplicativo cliente a ignorar a validação do certificado SSL/TSL do Servidor

# spring.kpa.hibernate.ddl-auto:
# nome: Banco de dados e tabelas devem ser ciradas com antecedência
# update: Banco de dados criado com antecedencia, tabelas podem ser criadas com antecedência, as tabelas são atualizadas pelo código (model)
#       toda vez que você crtia novos atributos
# create-drop: Banco de dados deve ser criado com antecedência, as tabelas são criadas automaticamente pelo código (model), entretanto as
#       as tabelas são excluídas toda vez que o servidor é "desligado"
# create: banco de dados deve ser criado com antecedência, quando o servidor carrega as tabelas são recriadas

# spring.jpa.show-sql= (true) As interações (DOL,DML) com o banco de dados são exibidas no console
#                       (false) As interações (DOL,DML) não seram exibidas no console